spring:
  # spring-data-redis 相关配置
  data:
    redis:
      host: localhost
      port: 6379
      password:  # 没有密码就空着
      database: 1
      timeout: 10s
      # 连接池
      lettuce:
        pool:
          max-active: 20
          max-idle: 10
          min-idle: 5
          max-wait: 5s